A simple template to create Beamer presentations. This is not a theme itself, but rather a template using beamer commands to customize the look of the presentation. The base is an standard Beamer theme.  
